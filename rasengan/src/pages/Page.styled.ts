import { Box, styled } from '@mui/material';


export const PageBox = styled(Box)(() => ({
  display: 'grid',
  width: '100%',
  height: '100%',
  maxWidth: '1440px',
  minWidth: '320px',
  margin: 'auto',
}))